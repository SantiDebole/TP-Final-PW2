<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulario con Bootstrap</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container mt-5 ">
    <!-- Encabezado h2 -->
    {{#pregunta}}
    <div class="p-4 bg-light rounded shadow-sm">
        <h2 class="mb-4">{{pregunta}}</h2>


        <!-- Formulario con checkbox -->
        <form action="partida/responder" method="post">
        {{#respuestas}}
            <div class="form-check mb-3">
                <input class="form-check-input" type="radio" id="{{idRta}}" name="respuesta">
                <label class="form-check-label" for="{{idRta}}">
                    {{respuesta}}
                </label>
            </div>
        {{/respuestas}}
            <input type="hidden" name="idPregunta" value="{{idPregunta}}">
            <!-- Botón submit -->
            <button type="submit" class="btn btn-primary">Responder</button>
        </form>

        <!-- Enlace de reporte -->
        <div class="mt-3">
            <a href="#" class="text-danger">Reportar pregunta</a>
        </div>
    </div>
        {{/pregunta}}
</div>

<!-- Bootstrap JS y dependencias -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
